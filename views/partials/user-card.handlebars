{{!-- To fill user feed cards in homepage. Also use for sidebar? --}}

{{!-- User, streak if any --}}
{{!-- Progress bar with points. x/100 pts for the day --}}
{{!-- Task list for the day(complete or incomplete) --}}
{{!-- Time of last completion --}}

<div class="list-item"> 
  <div class="row h-2">
        <img class="mb-4 col-2" src="/assets/images/PlaceholderPFP.png" alt="user picture">
        <h5 class="mt-2 col-2">{{this.username}}</h5>
<span class="offset-5">
  Last Task Completion:<br>
  {{#if this.lastCompletedTask}}
    {{formatDate this.lastCompletedTask.userTask.completion_time 'h:mm a'}}
  {{else}}
    No tasks completed yet
  {{/if}}
</span>
  </div>
      <ul class="list-group list-group-flush">
      <li class="list-group-item">Total points: {{this.total_points}}</li>
      <li>
      <div class="progress">
        <div class="progress-bar progress-bar-striped progress-bar-animated" role="progressbar" aria-valuenow="{{this.completionPercentage}}" aria-valuemin="0" aria-valuemax="100" style="width: {{this.completionPercentage}}%"></div>
      </div>
      </li>
    {{#each this.tasks}}
    <li class="list-group-item container">
      <div class="row">
        <h5 class="col-1"></h5>
        <div class="col-8">
          <p class="text-center">
            {{this.task_name}}
            {{#if this.userTask.completed}}
              Finished at {{formatDate this.userTask.completion_time 'h:mm a'}}
                      </div>
      <h5 class="daily col-3">+§ {{this.points}}</h5>
      </div>
            {{else}}
              Not Finished
            {{/if}}
          </p>
        {{!-- </div>
      <h5 class="daily col-3">+§ {{this.points}}</h5>
      </div> --}}
    </li>
    {{/each}}
  </ul>
</div>
  